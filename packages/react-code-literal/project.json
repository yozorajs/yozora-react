{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "react-code-literal",
  "sourceRoot": "packages/react-code-literal/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/react-code-literal",
        "parallel": false,
        "commands": ["rimraf lib"]
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["clean", "^build"],
      "options": {
        "cwd": "packages/react-code-literal",
        "parallel": false,
        "sourcemap": true,
        "commands": ["cross-env ROLLUP_CONFIG_TYPE=lib rollup -c ../../rollup.config.mjs"]
      },
      "configurations": {
        "production": {
          "sourceMap": false,
          "env": {
            "NODE_ENV": "production"
          }
        }
      }
    },
    "watch": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/react-code-literal",
        "parallel": false,
        "sourcemap": true,
        "commands": ["cross-env ROLLUP_CONFIG_TYPE=lib rollup -c ../../rollup.config.mjs -w"]
      }
    }
  }
}
